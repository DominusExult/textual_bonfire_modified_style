<div type="{{lineType}}"
	highlight="{{highlightAttributeRepresentation}}"
	class="line raw {{lineClassAttributeRepresentation}}"
	nicktype="{{nicknameType}}"
	id="line{{lineNumber}}"
	{{#isEncrypted}}
			encrypted="true"
	{{/isEncrypted}}
	time="{{lineRenderTime}}">

	<div class="nick"
		data-nick="{{nickname}}"
		onmouseover="BonfireHelpers.highlight_nick('{{nickname}}')"
		onmouseout="BonfireHelpers.unhighlight_nick('{{nickname}}')">
	<span class="sender"
  		ondblclick="Textual.nicknameDoubleClicked()"
  		oncontextmenu="Textual.openStandardNicknameContextualMenu()"
  		type="{{nicknameType}}"
  		nick="{{nickname}}"
		nickname="{{nickname}}"
  		{{#nicknameColorHashingEnabled}}
  			colornumber="{{nicknameColorNumber}}"
  		{{/nicknameColorHashingEnabled}}>{{formattedNickname}}</span>
	</div>
	<div class="msg">
		{{#isEncrypted}}
		{{{encryptedMessageLockTemplate}}}
		{{/isEncrypted}}
		{{#isRemoteMessage}}
			<p>
		{{/isRemoteMessage}}
			
		{{^isRemoteMessage}}
			<p type="{{nicknameType}}">
		{{/isRemoteMessage}}
		<span class="time">{{formattedTimestamp}}</span>
		<span class="message">
			{{{formattedMessage}}}

			{{#inlineMediaAvailable}}
			{{#inlineMediaArray}}
				<span class="inlineImageCell" id="inlineImage-{{anchorInlineImageUniqueID}}" style="display: none;">
					<span class="closeButton" href="#" onclick="Textual.toggleInlineImage('{{anchorInlineImageUniqueID}}', false);">×</span>
					
				<a href="{{{anchorLink}}}" onclick="return InlineImageLiveResize.negateAnchorOpen()">
					<img src="{{{imageURL}}}" class="image" style="max-width: {{preferredMaximumWidth}}px;" />
				</a>
			</span>
			{{/inlineMediaArray}}
			{{/inlineMediaAvailable}}
		</span>
		</p>
	</div>
</div>